## florr-auto-farm

⚠️ **风险声明**:游戏自动化脚本可能违反 florr.io 的服务条款,存在账号被封的可能。是否使用、后果如何,请自行判断、自行承担。

### 怎么用

下载 `florr-auto-farm-*-win64.zip`,把**整个文件夹**解压出来(别只拿出 exe),双击 `florr-auto-pathing.exe`。需要 Windows 10/11 和 Google Chrome。上手步骤见 README。

### 注意

- 程序会通过 Chrome 调试接口往 florr.io 页面注入脚本,并改写页面里两个设置字节(反转攻击键 / 反转防御键);详见 README 的「它对游戏页面做了什么」。
- 新建账号、点账号页的「登录」、调度器切换账号(含点「开始调度」后进入第一个时块)时,会**强制结束电脑上所有 Chrome 进程**,别的 Chrome 窗口和没保存的标签页会丢,使用前先保存好。
- 程序拉起的 Chrome 开着本机调试端口(`--remote-debugging-port`,允许任意来源连接),别在这个 Chrome 里打开不信任的网站。
- 杀毒软件可能误报(PyInstaller 打包的程序常见),加白名单,或者改用源码运行。
- 把文件夹发给别人之前,**先删掉 `chrome-profiles\`**——里面是你账号的登录态。
- AFK 弹窗处理是独立程序,首次启用时会询问是否下载(约 350MB)。
- 坐标按 1920×1080 标定,非 16:9 的窗口可能有偏差。
- 运行时它会接管鼠标和键盘、截屏读小地图,游戏要全屏放在最前面;时块里的换装、蚁穴地图没有在真机上验证过;自动换服之后落在哪个生态区也没有确认过。
